p,q=map(int,input().split())
k=list(map(int,input().split()))
for i in range(q):
    l,r=map(int,input().split())
    g=k[l-1:r]
    g.sort()
    u=len(bin(g[-1]))-2
    for i in range(u):
        a.append[0]
    for i in range(len(g)):
            
        
        
